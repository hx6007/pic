/*
 *
 * UserPackEditPage actions
 *
 */

import {
  DEFAULT_ACTION,
} from './constants';
// import { createAction } from '../../utils/actionCreator';

export function defaultAction() {
  return {
    type: DEFAULT_ACTION,
  };
}

