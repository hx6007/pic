// import { fromJS } from 'immutable';
// import { selectEffectDiagramListPageDomain } from '../selectors';

describe('selectEffectDiagramListPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
