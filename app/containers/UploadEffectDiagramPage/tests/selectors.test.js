// import { fromJS } from 'immutable';
// import { selectUploadEffectDiagramPageDomain } from '../selectors';

describe('selectUploadEffectDiagramPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
